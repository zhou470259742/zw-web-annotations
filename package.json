{
  "name": "zw-web-annotations",
  "version": "0.31.4",
  "private": true,
  "type": "module",
  "description": "AI agent skill: install web element batch-annotation into a frontend project with framework auto-detection. This directory is the single maintained copy (component source included).",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "install:runtime": "node scripts/cli.mjs install"
  },
  "engines": {
    "node": ">=20"
  }
}
